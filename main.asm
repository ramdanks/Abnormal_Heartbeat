;Port Assignment:
;2.0 - 2.6 = Seven Segment

SEVSEG	EQU	P1

LEDR	EQU	P2.0
LEDY	EQU	P2.1
LEDG	EQU	P2.2

RS	EQU	P2.3
RW	EQU	P2.4
ENB	EQU	P2.5

LCD16	EQU	P0
L_USE	EQU	038H
L_CLR	EQU	001H
L_LN1	EQU	080H
L_LN2	EQU	0C0H

;=====! (DATA) !=====;
	ORG	300H

NUMS:	DB	11111111B	;NULL
	DB	11000000B	;0
	DB	11111001B	;1
	DB	10100100B	;2
	DB	10110000B	;3
	DB	10011001B	;4
	DB	10010010B	;5
	DB	10000010B	;6
	DB	11111000B	;7
	DB	10000000B	;8
	DB	10010000B	;9

STR:	DB	'Test', 0

;=====! (SUBROUTINE) !=====;
	ORG	400H

SHOW7:	MOV	A, R6
	MOV	B, #4
	SL:	RL	A
		DJNZ	B, SL
	ADD	A, R7
	MOV	SEVSEG, A
	RET

SSTR:	SETB	SCON.6
	MOV	TMOD, #20H
	MOV	TH1, R0
	SETB	TR1

	SEND:	CLR	A
		MOVC	A, @A+DPTR
		JZ	S_RET
		MOV	SBUF, A
	WAIT:	JNB	TI, WAIT
		CLR	TI

	INC	DPTR
	SJMP	SEND
	S_RET:	RET

COUNT:

RESET:	MOV P0, #0FFH
	MOV P1, #0FFH
	MOV P2, #0FFH
	MOV P3, #0
	RET

;========! (MAIN) !========;
	ORG	00H




MOV A, #100
MOV TMOD, #01H
TIMER:	MOV 	TH0, #0D8H
	MOV 	TL0, #0F0H
	SETB 	TR0
WAIT:	JB	TF0, WAIT
	CLR 	TR0
	CLR 	TF0
	DJNZ 	A, TIMER


